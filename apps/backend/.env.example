# Banco interno (PostgreSQL do Docker)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=querycraft
DB_USER=querycraft
DB_PASS=querycraft_dev

# JWT
JWT_SECRET=report_builder_jwt_secret_super_secreto_2024
JWT_EXPIRES_IN=15m
JWT_REFRESH_SECRET=report_builder_refresh_secret_diferente_2024
JWT_REFRESH_EXPIRES_IN=7d

# Criptografia — precisa ter exatamente 32+ caracteres
ENCRYPTION_KEY=report_builder_encryption_key_32ch

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# App
PORT=3000
NODE_ENV=development